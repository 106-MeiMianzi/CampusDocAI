<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campusdoc.service.mapper.CardReplacementMapper">

    <resultMap id="CardReplacementMap" type="com.campusdoc.service.entity.CardReplacementRequestEntity">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="requestType" column="request_type"/>
        <result property="contactPhone" column="contact_phone"/>
        <result property="pickupLocation" column="pickup_location"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO card_replacement_request (user_id, request_type, contact_phone, pickup_location, status)
        VALUES (#{userId}, #{requestType}, #{contactPhone}, #{pickupLocation}, #{status})
    </insert>

    <select id="findByIdAndUserId" resultMap="CardReplacementMap">
        SELECT * FROM card_replacement_request
        WHERE id = #{id} AND user_id = #{userId}
    </select>

    <update id="updateStatus">
        UPDATE card_replacement_request
        SET status = #{status}
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <select id="listByUser" resultMap="CardReplacementMap">
        SELECT * FROM card_replacement_request
        WHERE user_id = #{userId}
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByUser" resultType="long">
        SELECT COUNT(1) FROM card_replacement_request
        WHERE user_id = #{userId}
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>
</mapper>
