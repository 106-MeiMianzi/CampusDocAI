<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campusdoc.chat.mapper.ChatMessageMapper">

    <resultMap id="MessageMap" type="com.campusdoc.chat.entity.ChatMessageEntity">
        <id property="id" column="id"/>
        <result property="conversationId" column="conversation_id"/>
        <result property="role" column="role"/>
        <result property="content" column="content"/>
        <result property="citationsJson" column="citations_json"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_message (conversation_id, role, content, citations_json)
        VALUES (#{conversationId}, #{role}, #{content}, #{citationsJson})
    </insert>

    <select id="listByConversationId" resultMap="MessageMap">
        SELECT * FROM chat_message WHERE conversation_id = #{conversationId} ORDER BY created_at ASC
    </select>

    <delete id="deleteByConversationId">
        DELETE FROM chat_message WHERE conversation_id = #{conversationId}
    </delete>
</mapper>
