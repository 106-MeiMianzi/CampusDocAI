<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campusdoc.document.mapper.DocumentMapper">

    <resultMap id="DocumentMap" type="com.campusdoc.document.entity.DocumentEntity">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="fileName" column="file_name"/>
        <result property="storagePath" column="storage_path"/>
        <result property="fileSize" column="file_size"/>
        <result property="status" column="status"/>
        <result property="errorMsg" column="error_msg"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO document (user_id, file_name, storage_path, file_size, status)
        VALUES (#{userId}, #{fileName}, #{storagePath}, #{fileSize}, #{status})
    </insert>

    <select id="findById" resultMap="DocumentMap">
        SELECT * FROM document WHERE id = #{id}
    </select>

    <select id="findByIdAndUserId" resultMap="DocumentMap">
        SELECT * FROM document WHERE id = #{id} AND user_id = #{userId}
    </select>

    <update id="updateStatus">
        UPDATE document SET status = #{status}, error_msg = #{errorMsg} WHERE id = #{id}
    </update>

    <select id="listByUser" resultMap="DocumentMap">
        SELECT * FROM document
        WHERE user_id = #{userId}
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByUser" resultType="long">
        SELECT COUNT(1) FROM document
        WHERE user_id = #{userId}
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <delete id="deleteByIdAndUserId">
        DELETE FROM document WHERE id = #{id} AND user_id = #{userId}
    </delete>

    <delete id="deleteById">
        DELETE FROM document WHERE id = #{id}
    </delete>

    <select id="findAccessibleById" resultMap="DocumentMap">
        SELECT d.* FROM document d
        LEFT JOIN `user` u ON u.id = d.user_id
        WHERE d.id = #{id}
          AND (d.user_id = #{userId} OR u.role = 'TEACHER')
    </select>

    <select id="listAccessible" resultMap="DocumentMap">
        SELECT d.* FROM document d
        LEFT JOIN `user` u ON u.id = d.user_id
        WHERE (d.user_id = #{userId} OR u.role = 'TEACHER')
        <if test="status != null">
            AND d.status = #{status}
        </if>
        ORDER BY d.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAccessible" resultType="long">
        SELECT COUNT(1) FROM document d
        LEFT JOIN `user` u ON u.id = d.user_id
        WHERE (d.user_id = #{userId} OR u.role = 'TEACHER')
        <if test="status != null">
            AND d.status = #{status}
        </if>
    </select>
</mapper>
